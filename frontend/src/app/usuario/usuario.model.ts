export class User {
    constructor(public id: number, public nome: string, public sobrenome: string, public telefone: string, public cep: string, public email: string, public senha: string, public cpf: string) {

    }
}